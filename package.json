{
  "name": "zaofan-workspace",
  "private": true,
  "workspaces": [
    "clients/*"
  ],
  "scripts": {
    "dev:web":  "npm run dev --workspace=clients/web",
    "dev:lite": "npm run dev --workspace=clients/lite",
    "build:web": "npm run build --workspace=clients/web"
  }
}
